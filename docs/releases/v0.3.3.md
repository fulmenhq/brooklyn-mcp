# Brooklyn MCP v0.3.3 Release Notes

**Date**: February 2026  
**Tag**: `v0.3.3`  
**Theme**: _SaaS Dashboard Unlock_ - Auth headers + analyst extraction

## 🎯 **Headline Benefits**

Brooklyn bridges AI to your private dashboards. **Extract structured data from Stripe, Mixpanel, Amplitude** - turning screenshots into JSON tables for analysis, planning, and automation.

**Before**: Public sites only, manual data entry.  
**After**: Auth tokens → paginated tables → analyst-ready datasets.

## 🚀 **New Capabilities**

### **Auth-Gated Browsing**

- `launch_browser(extraHttpHeaders={Authorization: Bearer...})` - Headers persist on all requests
- `BROOKLYN_HTTP_HEADERS` env fallback for process-local tokens
- Works with authbolt credential grabber (loose-coupled)

### **Analyst Data Extraction**

- `extract_table_data` - HTML tables → JSON/CSV (handles rowspan/colspan)
- `paginate_table` - Auto-collect multi-page dashboards (explicit next selector)

### **Network Inspector**

- `inspect_network` - Recent requests with redacted headers ([REDACTED] by default)
- `includeRaw=true` gated (BROOKLYN_FULL_HEADER_SUPPORT=true + audit)

## 📊 **User Impact**

> **AI Analyst Persona**: "Pull Stripe MRR tables + Mixpanel cohorts → Auto-generate quarterly report"

**Real-world Wins**:

- Competitive analysis from Amplitude dashboards
- Feature planning from customer SaaS usage data
- Documentation from gated help centers

## 🛠️ **Technical Changes**

- 4 new tools + schema extension (inventory regenerated)
- Network event buffering (memory-only, TTL 5min)
- Header resolution: MCP param > env var > none
- Tests: auth scenarios, table edge cases, pagination

**Upgrades**:

```
bun upgrade
git pull origin main
bun run check-all
bun run install  # Refresh CLI
```

## 🔒 **Security Highlights**

- Headers redacted in logs/tools by default
- Raw inspect gated (env + audit log)
- No disk storage (v0.3.4 adds encrypted files)

## 📈 **What's Next (v0.3.4)**

- Encrypted header storage per-team
- Pagination auto-detect
- `brooklyn_list_headers` tool

**Start Fast. Unlock SaaS.** 🌉
