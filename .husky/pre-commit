#!/bin/sh

# Brooklyn Pre-commit Hook
# Delegates to Makefile for FulmenHQ ecosystem consistency

echo "🌉 Brooklyn Pre-commit Checks..."

make precommit || {
  echo ""
  echo "❌ Pre-commit checks failed. Please fix before committing."
  echo ""
  echo "💡 Quick fixes:"
  echo "   - Format issues: 'make fmt'"
  echo "   - Type errors: 'make typecheck'"
  echo "   - Lint issues: 'make lint'"
  echo "   - Test failures: 'make test-unit'"
  exit 1
}

echo "✅ All pre-commit checks passed!"
